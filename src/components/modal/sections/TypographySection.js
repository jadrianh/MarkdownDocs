import { renderTypographyPage } from '../pages/TypographyPage.js';

/**
 * Typography Section Template (Backward compatibility wrapper)
 * @returns {string} HTML markup string
 */
export function renderTypographySection() {
    return renderTypographyPage();
}
